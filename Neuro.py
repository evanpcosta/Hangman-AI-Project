import Main


class Neuro:

    def parse(self):
        N = 3
        file = open(self, "r")  # the a opens it in read mode
        for i in range(N):
            line = file.readline()
            #insert
            #button next
            #Guess
            #if correct continue
            #if failed continue
            print(line)
        file.close()

    def insert(self):
        return

    def guess(self):
        #number of guesses int
        #Things that you can guess string
        #insert into new entry
        #button guess
        return